version: "2.2"
services:
  web:
    build:
      context: ./Server
    command: npm run dev
    volumes:
      - .:/tmp
    ports:
      - "8080:5000"
# services:
#   client_web:
#     ports:
#       - "8081:5000"
#     volumes:
#       - #same as client_mobile
#     depends_on:
#       server:
#         condition: service_healthy
#       client_mobile:
#         condition: service_healthy
#   server:
#     ports:
#       - "8080:5000"
#     healthcheck:
#       #TO DO
#   client_mobile:
#     build: .
#     volumes:
#       - #same as client_web
#     healthcheck:
#       #TO DO
